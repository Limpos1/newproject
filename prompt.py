# -*- coding: utf-8 -*-
"""
목차 파싱용 프롬프트 템플릿.
- PDF 텍스트 기반 파싱과 사진(vision) 기반 파싱 둘 다 동일한 스키마를 쓴다.
- endPage는 LLM에게 계산시키지 않는다 (백엔드에서 order 기반으로 계산).
"""

TOC_PARSING_PROMPT = """너는 책 목차를 분석해서 구조화된 데이터로 변환하는 파서다.

이미지가 여러 장 주어질 수 있다. 목차가 한 장에 다 안 들어가서 여러 장으로 나눠
촬영된 경우(예: 목차 1페이지 사진 + 2페이지 사진), 여러 장을 각각 별개의 목차로
취급하지 말고 순서대로 이어붙여 하나의 목차로 합쳐서 처리해라. order 번호도
전체를 통틀어 이어서 매긴다 (두 번째 이미지의 첫 항목이 order=1로 다시 시작하면 안 된다).

책이 여러 권(1권/2권, Vol.1/Vol.2 등)으로 나뉘어 있고, 권이 바뀌면서 페이지 번호가
"1-13", "2-5"처럼 권 번호가 앞에 붙거나, 권이 바뀌는 지점부터 페이지 번호가
다시 1(또는 작은 숫자)부터 시작하는 경우가 있다. 이 경우:
- 각 최상위 챕터에 그 항목이 속한 권 번호를 "volume" 필드(1부터 시작하는 정수)로
  반드시 표시해라. 소단원(subunits)은 항상 자기 챕터와 같은 권이므로 별도 표시가
  필요 없다.
- startPage/endPage에는 권 번호를 빼고 순수한 페이지 숫자만 넣어라 (예: "2-5" ->
  volume=2, startPage=5). 권 번호를 페이지 숫자에 합치거나 이어붙이지 마라.
- 권 표시가 아예 없는 단일 권 책이면 모든 항목의 volume을 1로 통일해라.
- 절대로 다른 권의 페이지 번호끼리 서로 비교해서 "순서가 이상하다"고 판단하지
  마라. 예를 들어 1권 마지막 챕터가 624페이지이고 2권 첫 챕터가 5페이지인 것은
  정상이다 (권이 바뀌면서 페이지 번호가 다시 시작된 것뿐, 오류가 아니다).

가장 중요한 주의사항 (제목-페이지번호 오배정 방지):
- 목차 이미지에서 페이지 번호는 시각적으로 항목 제목의 '한 줄 아래' 또는
  '줄 사이'에 걸쳐 보이는 경우가 많다. 이때 페이지 번호를 실제로는 다음 항목의
  것인데 현재 항목의 것으로 착각해서, 모든 항목의 페이지 번호가 한 칸씩
  밀려 배정되는 실수가 자주 발생한다. 이 실수를 반드시 피해야 한다.
- 따라서 아래 순서를 반드시 지켜서 작업해라:
  1) 먼저 보이는 모든 항목의 "제목"만 위에서 아래 순서대로 나열한다
     (이 단계에서는 페이지 번호를 전혀 보지 않는다).
  2) 그 다음 보이는 모든 "페이지 번호"만 위에서 아래(왼쪽에서 오른쪽) 순서대로
     따로 나열한다 (이 단계에서는 어느 제목에 속하는지 생각하지 않는다).
  3) 제목 목록의 개수와 페이지 번호 목록의 개수를 비교한다. 개수가 안 맞으면
     둘 중 하나를 놓친 것이니 처음부터 다시 확인한다.
  4) 개수가 맞으면 같은 순서(1번째-1번째, 2번째-2번째, ...)로 1:1로 짝짓는다.
     이때 각 쌍이 실제 이미지에서 같은 가로줄(행)에 있는 게 맞는지
     반드시 한 번 더 눈으로 확인한다. 확인 없이 그냥 순서대로만 짝짓지 마라.
  5) 짝짓기가 끝나면 startPage들이 항목 순서대로 오름차순인지 확인한다
     (단, 위에서 설명한 것처럼 권이 바뀌는 지점은 예외다). 같은 권 안인데도
     오름차순이 아니거나 앞뒤 항목과 비교했을 때 부자연스럽게 크게 건너뛰는
     값이 있다면, 밀림(off-by-one shift)이 의심되는 것이니 3)~4) 단계로
     돌아가 다시 확인한다.
  6) 대단원/PART 헤더(굵은 구분 바 등)가 이미지 안에 2개 이상 있다면, 반드시
     서로 비교해라. 한 헤더 옆에는 페이지 번호가 없는데 다른 헤더 옆에는
     있는 것처럼 보인다면, 같은 책 안에서 서식이 갑자기 바뀌었을 가능성보다
     번호를 잘못 배정했을 가능성이 훨씬 크다. 이 경우 번호가 있어 보였던
     헤더도 실제로는 번호가 없는 것으로 보고, 그 번호는 바로 아래 첫 번째
     하위 챕터의 것으로 재배정해라.

작업:
1. 입력된 목차(텍스트 또는 이미지)에 보이는 모든 항목(장/절/부록 등)을 순서대로 추출한다.
2. 각 항목의 계층 구조(대단원-소단원)를 파악한다.
3. 각 항목 옆에 표기된 페이지 번호를, 위 "가장 중요한 주의사항"의 절차를 따라
   startPage로 추출한다.
   - endPage는 계산하지 말고 원본에 표기된 시작 페이지 번호만 정확히 추출해라.
4. 각 항목을 다음 기준으로 분류해 contentType을 부여한다.
   - CONTENT: 실제 학습 내용을 다루는 장/절 (예: "3장 알고리즘", "3.1 배열")
   - NON_CONTENT: 학습 내용이 아닌 항목
     (머리말, 들어가며, 서문, 프롤로그, 에필로그, 저자 소개, 감사의 글, 일러두기,
      부록, 찾아보기, 색인, 참고문헌, 연습문제 정답 등)
5. 각 CONTENT 항목에 대해 제목만 보고 대략적인 난이도(estimatedDifficulty)를
   "easy" / "medium" / "hard" 중 하나로 추정한다. 근거가 부족하면 "medium"으로 둔다.
6. 페이지 번호가 보이지 않거나 읽을 수 없는 항목, 혹은 어느 줄에 속하는지
   확신할 수 없는 항목은 억지로 추측하지 말고 startPage를 null로 둔다.
7. 최상위 항목과 하위 항목 모두 order(1부터 시작하는 형제 순서 번호)를 부여한다.
   order는 각 권 안에서 다시 시작하지 말고 문서 전체를 통틀어 이어서 매긴다.

출력 형식:
- 위 "가장 중요한 주의사항" 1)~6) 단계의 과정은 짧게 텍스트로 정리해서 먼저 보여줘도 된다.
- 단, 최종 결과는 반드시 ```json 코드블록 하나로 감싸서 출력하고, 그 코드블록
  안에는 아래 스키마에 맞는 JSON 외의 어떤 텍스트도 넣지 마라.
- 목차로 보이는 내용이 전혀 없으면 코드블록 안에 {"chapters": [], "error": "no_toc_detected"}를 반환한다.

스키마:
{
  "chapters": [
    {
      "order": 1,
      "title": "string",
      "volume": 1,
      "contentType": "CONTENT" | "NON_CONTENT",
      "startPage": number | null,
      "estimatedDifficulty": "easy" | "medium" | "hard",
      "subunits": [
        { "order": 1, "title": "string", "startPage": number | null }
      ]
    }
  ]
}

이제 아래 입력을 분석해라.
"""